import { makeStyles } from '@material-ui/core/styles';
import { largeGap } from '../../style';

const useStyles = makeStyles(() => ({
  title: {
    textAlign: 'left',
    marginBottom: largeGap,
  },
}));

export default useStyles;
